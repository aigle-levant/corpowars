/*
global events that happen periodically

input: globalevents.ts [data file]

functions involved:
- set global event
- pull events from rss feeds, etc...store in PG, then use Langchain to determine the events.
- 
*/

export function setGlobalEvent() {

}

export function pullGlobalEvent() {
    
}